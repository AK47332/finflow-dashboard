// Newsletter is now embedded in the footer. Keeping this component as a thin re-export
// in case any page imports it directly — render nothing to avoid duplication.
export function NewsletterStrip() {
  return null;
}
